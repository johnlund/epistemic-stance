# Epistemic Stance Project Dependencies
# =====================================

# API and External Services
anthropic>=0.18.0

# Data Sources
convokit>=3.0.0
datasets>=2.16.0

# Core ML and Deep Learning
torch>=2.0.0
transformers>=4.36.0
accelerate>=0.25.0
tokenizers>=0.14.0  # For faster tokenization

# Data Processing
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# Progress bars and logging
tqdm>=4.66.0

# Optional: for experiment tracking (uncomment if using)
wandb>=0.15.0
# tensorboard>=2.14.0

# Optional: for model uploads to HuggingFace Hub (uncomment if using)
huggingface_hub>=0.19.0

# Optional: for hyperparameter tuning (uncomment if using)
# optuna>=3.3.0
