# Epistemic Stance Project Dependencies
# =====================================

# API and External Services
anthropic>=0.18.0

# Data Sources
convokit>=3.0.0
datasets>=2.16.0

# Core ML and Deep Learning
torch>=2.0.0
transformers>=4.44.0
accelerate>=0.25.0
tokenizers>=0.14.0  # For faster tokenization

# Data Processing
pandas>=2.0.0
numpy>=1.24.0,<2.0.0  # Pin to <2.0 for compatibility with system scipy
scikit-learn>=1.3.0

# Progress bars and logging
tqdm>=4.66.0

# Experiment tracking
wandb>=0.15.0

# HuggingFace Hub for model uploads
huggingface_hub>=0.19.0

# Flash Attention (install separately - requires CUDA)
# pip install flash-attn --no-build-isolation

# Optional: for hyperparameter tuning (uncomment if using)
# optuna>=3.3.0
